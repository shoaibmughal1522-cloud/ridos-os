name: Build RIDOS OS ISO

on:
  push:
    branches: [ main ]
    tags: [ "v*" ]
  workflow_dispatch:

jobs:
  build-iso:
    runs-on: ubuntu-24.04
    permissions:
      contents: write

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            debootstrap squashfs-tools xorriso \
            grub-pc-bin grub-efi-amd64-bin grub2-common \
            mtools dosfstools gnupg2 curl wget \
            imagemagick librsvg2-bin syslinux-utils rsync \
            isolinux syslinux syslinux-common
          echo "Done"

      - name: Bootstrap and Configure RIDOS
        run: |
          # 1. Bootstrap
          sudo debootstrap --arch=amd64 bookworm chroot http://deb.debian.org/debian/
          
          # 2. Setup Repos for Firmware
          sudo tee chroot/etc/apt/sources.list << 'EOF'
          deb http://deb.debian.org/debian bookworm main contrib non-free non-free-firmware
          deb http://deb.debian.org/debian bookworm-updates main contrib non-free non-free-firmware
          deb http://security.debian.org/debian-security bookworm-security main contrib non-free non-free-firmware
          EOF

          # 3. Mount and Install Full Package List
          sudo mount --bind /dev chroot/dev
          sudo mount --bind /proc chroot/proc
          sudo mount --bind /sys chroot/sys
          
          sudo chroot chroot apt-get update
          sudo chroot chroot apt-get install -y --no-install-recommends \
            live-boot live-boot-initramfs-tools linux-image-amd64 initramfs-tools \
            xfce4 xfce4-terminal lightdm lightdm-gtk-greeter firefox-esr \
            sudo network-manager bluetooth bluez pulseaudio alsa-utils \
            firmware-linux firmware-linux-nonfree firmware-misc-nonfree \
            intel-microcode amd64-microcode neofetch htop git vim nano curl wget \
            pciutils usbutils
          
          # 4. Branding & User Setup
          echo "ridos-os" | sudo tee chroot/etc/hostname
          sudo chroot chroot useradd -m -s /bin/bash -G sudo ridos
          echo "ridos:ridos" | sudo chroot chroot chpasswd
          echo "root:ridos" | sudo chroot chroot chpasswd
          
          # 5. Cleanup
          sudo umount chroot/sys chroot/proc chroot/dev

      - name: Build SquashFS
        run: |
          sudo mksquashfs chroot filesystem.squashfs -comp xz -e boot
          mkdir -p iso/live iso/boot/grub iso/isolinux
          cp filesystem.squashfs iso/live/
          
          # Extract Kernel and Initrd
          KERNEL=$(ls chroot/boot/vmlinuz-* | sort -V | tail -1)
          INITRD=$(ls chroot/boot/initrd.img-* | sort -V | tail -1)
          cp "$KERNEL" iso/live/vmlinuz
          cp "$INITRD" iso/live/initrd

          # GRUB Config (UEFI boot)
          cat > iso/boot/grub/grub.cfg << 'EOF'
          set default=0
          set timeout=5
          menuentry "RIDOS OS v1.1.0 Baghdad" {
            linux /live/vmlinuz boot=live quiet splash
            initrd /live/initrd
          }
          menuentry "RIDOS OS (Safe Graphics)" {
            linux /live/vmlinuz boot=live nomodeset quiet splash
            initrd /live/initrd
          }
          menuentry "RIDOS OS (Copy to RAM)" {
            linux /live/vmlinuz boot=live toram quiet splash
            initrd /live/initrd
          }
          EOF

          # ── isolinux boot files (Legacy BIOS + Rufus detection) ──
          cp /usr/lib/ISOLINUX/isolinux.bin               iso/isolinux/
          cp /usr/lib/syslinux/modules/bios/ldlinux.c32   iso/isolinux/
          cp /usr/lib/syslinux/modules/bios/libcom32.c32  iso/isolinux/
          cp /usr/lib/syslinux/modules/bios/libutil.c32   iso/isolinux/
          cp /usr/lib/syslinux/modules/bios/vesamenu.c32  iso/isolinux/

          # isolinux config — mirrors GRUB entries above
          cat > iso/isolinux/isolinux.cfg << 'EOF'
          UI vesamenu.c32
          TIMEOUT 50
          PROMPT 0

          MENU TITLE RIDOS OS v1.1.0 Baghdad

          LABEL live
            MENU LABEL RIDOS OS v1.1.0 Baghdad (Live)
            KERNEL /live/vmlinuz
            APPEND initrd=/live/initrd boot=live quiet splash

          LABEL safe
            MENU LABEL RIDOS OS (Safe Graphics Mode)
            KERNEL /live/vmlinuz
            APPEND initrd=/live/initrd boot=live nomodeset quiet splash

          LABEL ram
            MENU LABEL RIDOS OS (Copy to RAM)
            KERNEL /live/vmlinuz
            APPEND initrd=/live/initrd boot=live toram quiet splash
          EOF

      - name: Build Hybrid ISO
        run: |
          # ── BIOS boot image (GRUB i386-pc, preserved from original) ──
          grub-mkimage -O i386-pc -o bios.core.img -p "/boot/grub" \
            iso9660 linux normal biosdisk search
          cat /usr/lib/grub/i386-pc/cdboot.img bios.core.img > bios.img

          # ── UEFI boot image (preserved from original) ──
          grub-mkimage -O x86_64-efi -o bootx64.efi -p "/boot/grub" \
            iso9660 linux normal search
          dd if=/dev/zero of=efi.img bs=1M count=4
          mkfs.vfat efi.img
          mmd -i efi.img ::/EFI ::/EFI/boot
          mcopy -i efi.img bootx64.efi ::/EFI/boot/bootx64.efi

          # ── Build hybrid ISO ──
          #
          # Boot priority order:
          #   1. isolinux  → Legacy BIOS (Rufus "Windows mode" trigger)
          #   2. bios.img  → GRUB BIOS fallback (--grub2-boot-info)
          #   3. efi.img   → UEFI
          #
          # Key flags for Rufus compatibility:
          #   -isohybrid-mbr isohdpfx.bin  → writes MBR hybrid signature Rufus looks for
          #   --grub2-mbr boot_hybrid.img  → GRUB2 protective MBR (kept from original)
          #   --mbr-force-bootable         → marks partition bootable in MBR table
          #
          sudo xorriso -as mkisofs \
            -iso-level 3 \
            -volid "RIDOS_OS" \
            \
            -isohybrid-mbr /usr/lib/ISOLINUX/isohdpfx.bin \
            --grub2-mbr /usr/lib/grub/i386-pc/boot_hybrid.img \
            --mbr-force-bootable \
            \
            -eltorito-boot isolinux/isolinux.bin \
            -eltorito-catalog isolinux/boot.cat \
            -no-emul-boot -boot-load-size 4 -boot-info-table \
            \
            -eltorito-alt-boot \
            -e boot/grub/bios.img \
            -no-emul-boot -boot-info-table --grub2-boot-info \
            \
            -eltorito-alt-boot \
            -e boot/grub/efi.img \
            -no-emul-boot \
            -isohybrid-gpt-basdat \
            \
            -o ridos-os-1.1.0-Baghdad-x86_64.iso \
            -graft-points \
            "/boot/grub/bios.img=bios.img" \
            "/boot/grub/efi.img=efi.img" \
            "/=iso"

          md5sum ridos-os-1.1.0-Baghdad-x86_64.iso > ridos-os-1.1.0-Baghdad-x86_64.iso.md5

      - name: Upload ISO
        uses: actions/upload-artifact@v4
        with:
          name: RIDOS-OS-Baghdad
          path: |
            ridos-os-1.1.0-Baghdad-x86_64.iso
            ridos-os-1.1.0-Baghdad-x86_64.iso.md5
